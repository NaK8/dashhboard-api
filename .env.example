# Database
DATABASE_URL=postgresql://username:password@localhost:5432/medical_dashboard

# JWT
JWT_SECRET=your-super-secret-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=development

# Webhook secret — set this same value in your WordPress webhook plugin
# so you can verify incoming webhooks are actually from your WordPress site
WEBHOOK_SECRET=your-webhook-secret-key

# CORS — your dashboard URL
DASHBOARD_URL=https://dashboard.wellhealthlabs.com
